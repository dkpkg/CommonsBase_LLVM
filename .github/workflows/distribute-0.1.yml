on:
  push:
    tags:
      - '0.1.*' # secrets are tied to version 0.1
  workflow_dispatch: # allow manual triggering from GitHub page

jobs:
  distribute:
    # The secrets created by `dksrc/dk0 prepare-version --ci github VERSION`
    # are tied to this environment only.
    environment: dk-distribution

    strategy:
      fail-fast: false
      matrix:
        include:
            # curl and build-essential are part of ubuntu-24.04 CI runner, so test with:
            #   docker run -v .:/work --workdir /work -it ubuntu:24.04 bash -c "apt update && apt install -y curl build-essential && sh ./dk0 distribute --actual-in-place --library CommonsBase_LLVM@0.1.99 part1 dist/linux.u"
            #   docker run -v .:/work --workdir /work -it quay.io/pypa/manylinux_2_28_x86_64:2026.04.08-5 sh ./dk0 distribute --actual-in-place --library CommonsBase_LLVM@0.1.99 part1 dist/linux.u
          - runs-on: ubuntu-latest
            distscript: dist/linux.u
            image: quay.io/pypa/manylinux_2_28_x86_64:2026.04.08-5 # glibc 2.28 + gcc 14.2.1: old glibc is highly compatible and works with Node v24.14.0; system gcc will not be needed once GCC bootstrapped in dk0.
            execution-abi: Linux_x86_64
          - runs-on: macos-latest
            distscript: dist/macos.u
            execution-abi: Darwin_arm64
          - runs-on: windows-latest
            distscript: dist/win32.u
            execution-abi: Windows_x86_64

    runs-on: ${{ matrix.runs-on }}
    container:
      image: ${{ matrix.image }}

    steps:
      - name: Harden Runner # Optional but recommended
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176 # v2.17.0
        with: { egress-policy: audit }
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Distribute Modules
        uses: diskuv/dk-distribute@v3
        with:
            pubkey: ${{ secrets.distribute_0_1_pubkey }}
            seckey: ${{ secrets.distribute_0_1_seckey }}
            options: -nosysinc
            store: none
            distscript: ${{ matrix.distscript }}
            execution-abi: ${{ matrix.execution-abi }}
            #   Use MlFront HEAD when CI build is not from a tag
            experimental-mlfront-ref: ${{ github.ref_type != 'tag' && 'HEAD' }}
            #   Uncomment when testing MlFront
            # experimental-mlfront-ref: HEAD

  combine:
    needs: distribute
    permissions:
      contents: write # for action-gh-release
      id-token: write # for actions/attest-build-provenance
      attestations: write # for actions/attest-build-provenance
      #   required but does not work in `act`! bug: https://github.com/nektos/act/issues/6025
      artifact-metadata: write # for actions/attest-build-provenance

    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner # Optional but recommended
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176 # v2.17.0
        with: { egress-policy: audit }
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Combine Modules
        uses: diskuv/dk-distribute/combine@v3

      # Attests the distribution, publishes the sigstore bundle for values.json
      # as a release asset, and creates the release. A consumer then fetches
      # that bundle beside values.json with a plain download, so importing this
      # release needs no GitHub API token.
      - name: Attest and Release
        uses: diskuv/dk-distribute/attest-release@v3
